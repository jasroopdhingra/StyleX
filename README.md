# Magic Patterns - Vite Template

This code was generated by [Magic Patterns](https://magicpatterns.com) for this design: [Source Design](https://www.magicpatterns.com/c/gsnfjcozzbggrpvinrms22)

## Getting Started

1. Run `npm install`
2. Run `npm run server` to start the local LLM-backed API (uses `OPENAI_API_KEY` if set).
3. In another terminal, run `npm run dev`
